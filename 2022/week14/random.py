import random

# Commentez/décommentez cette ligne. Si random.seed(1) n'est pas commenté, les nombres générés seront toujours les mêmes.
random.seed(1)

# Affiche un nombre entre 0 et 1
print(random.random())
print(random.random())

# Affiche un nombre entre 0 et 10
print(random.randint(0,10))
print(random.randint(0,10))
